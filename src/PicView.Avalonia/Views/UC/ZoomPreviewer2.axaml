<UserControl
    Focusable="False"
    x:Class="PicView.Avalonia.Views.UC.ZoomPreviewer2"
    x:DataType="viewModels1:TabViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:customControls="clr-namespace:PicView.Avalonia.CustomControls"
    xmlns:viewModels1="clr-namespace:PicView.Core.ViewModels;assembly=PicView.Core"
    xmlns:vm="clr-namespace:PicView.Avalonia.ViewModels"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border
        Background="{DynamicResource MainBackgroundColor}"
        BorderBrush="{DynamicResource MainBorderColor}"
        BorderThickness="1"
        CornerRadius="6,6,1,1"
        x:Name="MainBorder">
        <StackPanel>
            <Border
                Background="{DynamicResource MainBackgroundColor}"
                BorderBrush="{DynamicResource MainBorderColor}"
                BorderThickness="0,0,1,1"
                CornerRadius="6,6,1,1">
                <Grid ColumnDefinitions="*,*">
                    <customControls:IconButton
                        Classes="altHover mainBtn"
                        Command="{CompiledBinding $parent[Window].((vm:MainViewModel)DataContext).Tools.ResetZoomCommand}"
                        CornerRadius="6,0,0,0"
                        Data="{StaticResource ReloadGeometry}"
                        Foreground="{DynamicResource MainTextColor}"
                        Grid.Column="0"
                        Height="22"
                        IconHeight="11"
                        IconWidth="11"
                        Padding="1"
                        ToolTip.Tip="{CompiledBinding $parent[Window].((vm:MainViewModel)DataContext).Translation.ResetZoom.Value}"
                        x:Name="ResetZoomButton" />

                    <customControls:IconButton
                        Classes="altHover mainBtn"
                        CornerRadius="0,6,0,0"
                        Data="{StaticResource CloseGeometry}"
                        Foreground="{DynamicResource MainTextColor}"
                        Grid.Column="1"
                        Height="22"
                        IconHeight="10"
                        IconWidth="10"
                        Padding="1"
                        ToolTip.Tip="{CompiledBinding $parent[Window].((vm:MainViewModel)DataContext).Translation.Close.Value}"
                        x:Name="CloseButton" />
                </Grid>
            </Border>
            <LayoutTransformControl>
                <LayoutTransformControl.LayoutTransform>
                    <RotateTransform Angle="{CompiledBinding CurrentModel.Value.Rotation}" />
                </LayoutTransformControl.LayoutTransform>
                <Panel>
                    <customControls:PicBox
                        ImageType="{CompiledBinding CurrentModel.Value.ImageType,
                                                    Mode=OneWay}"
                        Source="{CompiledBinding CurrentModel.Value.Image}"
                        x:Name="OverlayImage">
                        <customControls:PicBox.RenderTransform>
                            <ScaleTransform ScaleX="{CompiledBinding $parent[Window].((vm:MainViewModel)DataContext).PicViewer.ScaleX.Value}" />
                        </customControls:PicBox.RenderTransform>
                    </customControls:PicBox>
                    <Canvas x:Name="OverlayCanvas">
                        <Rectangle
                            Fill="Transparent"
                            Stroke="{DynamicResource AccentColor}"
                            StrokeThickness="2"
                            x:Name="ViewportBorder" />
                        <Canvas.RenderTransform>
                            <ScaleTransform ScaleX="{CompiledBinding $parent[Window].((vm:MainViewModel)DataContext).PicViewer.ScaleX.Value}" />
                        </Canvas.RenderTransform>
                    </Canvas>
                </Panel>
            </LayoutTransformControl>
        </StackPanel>
    </Border>
</UserControl>