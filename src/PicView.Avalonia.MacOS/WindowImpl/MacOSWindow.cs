using PicView.Avalonia.MacOS.Views;
using PicView.Avalonia.ViewModels;

namespace PicView.Avalonia.MacOS.WindowImpl;
    public static class MacOSWindow
    {
        public static async Task ToggleFullscreen(MacMainWindow? mainWindow, MainViewModel? vm, bool saveSetting)
        {
            throw new NotImplementedException();
        }

        public static async Task Restore(MacMainWindow? mainWindow, MainViewModel? vm)
        {
            throw new NotImplementedException();
        }

        public static async Task Fullscreen(MacMainWindow? mainWindow, MainViewModel? vm, bool saveSetting)
        {
            throw new NotImplementedException();
        }

        public static async Task ToggleMaximize(MacMainWindow? mainWindow, MainViewModel? vm, bool saveSetting)
        {
            throw new NotImplementedException();
        }

        public static async Task Maximize(MacMainWindow? mainWindow, MainViewModel? vm, bool saveSetting)
        {
            throw new NotImplementedException();
        }
    }